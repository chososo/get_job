# Quant Career Desk — 프로젝트 개발 지침서

작성일: 2026-09-08 / 기본 시간대: Asia/Seoul

## 1. 목표와 사용자 조건

국내 중심의 주니어 금융 커리어 탐색, 지원 일정, 경험과 지원서 작성을 하나의 개인 웹앱에서 관리한다. GitHub Pages에 HTML 웹앱을 배포한다. 국내와 해외 공고 페이지, 작성 도우미, 저장한 지원서 페이지를 분리한다.

- 지원 수준: 인턴, 신입, 신입 지원이 명시된 혼합 채용. 경력 요건 불명은 확인 필요로 분리한다.
- 분야: 퀀트 리서치/개발, 계량 자산운용, 자산배분/멀티에셋, ETF/인덱스 운용, 지수개발, ETF LP, 시장·신용·투자리스크, 모델 검증, 성과분석, 파생상품평가.
- 국내 우선. 해외는 영국, 홍콩, 싱가포르, 폴란드, 일본만 포함한다.
- 일본어·폴란드어·중국어를 못한다. 해당 언어 필수 공고는 제외하고 우대는 표시한다. 영어로 쓰였다는 이유만으로 영어만으로 지원 가능하다고 단정하지 않는다.
- 학위, 졸업 시점, 영어 수준, 비자/취업허가 보유 여부는 아직 미확인. 임의로 가정하지 않는다.
- 일반 영업/마케팅, 일반 내부통제/준법, 단순 펀드회계는 핵심 목록에서 제외한다. ETF LP와 운용사 ETF 운용은 별도 직무로 표시한다.

## 2. 반드시 지켜야 할 개인정보 경계

GitHub에는 앱 코드, 공개 채용 사실 정보, 공개 출처 URL, 공개 수집 상태만 저장한다. 경험, 자소서, CV, 메모, 지원 상태, 즐겨찾기, 개인 일정, 비밀번호, 토큰, AI 입력/출력은 올리지 않는다. private 저장소도 개인정보 저장 장소로 취급하지 않는다.

- 개인 자료는 IndexedDB에 AES-GCM으로 암호화한다. 사용자 비밀번호에서 PBKDF2-SHA256으로 키를 파생하고, 키와 비밀번호는 메모리에만 둔다. 문서 저장마다 새로운 IV를 사용한다.
- 최초 사용 시 로컬 보관함을 만들고 이후 비밀번호로 연다. 비밀번호 복구 서버는 없다. 이 점은 생성 화면에서 안내한다.
- 브라우저 저장은 기기·브라우저·웹 출처별로 다르다. GitHub Pages와 localhost는 서로 다른 보관함이다. 브라우저 데이터 삭제 시 손실될 수 있으므로 암호화 백업 내보내기/가져오기를 제공한다.
- 암호화 백업은 사용자가 다운로드 위치를 정한다. 클라우드 동기화 폴더가 아닌 로컬 폴더를 권장한다. 평문 문서 내보내기는 사용자가 직접 누른 경우에만 수행한다.
- 외부 분석, 광고, 원격 폰트, 외부 스크립트, 오류수집 SaaS를 넣지 않는다. 문서 내용은 URL, 콘솔, 테스트 산출물에 넣지 않는다.
- CSP를 적용하고 사용자 입력·외부 공고는 HTML로 실행하지 않는다. 링크는 http/https만 허용하고 새 창 링크는 noopener/noreferrer를 설정한다.
- 같은 GitHub Pages 출처의 다른 프로젝트는 브라우저 출처를 공유할 수 있다. 고유 DB 이름은 충돌을 피하지만 보안 격리를 보장하지 않는다. 강한 격리가 필요하면 별도 도메인 또는 로컬 전용 사용을 권장한다.

## 3. 시스템 구성

### 공개 웹앱

`web/`만 Pages 배포 대상으로 삼는다. 정적 HTML/CSS/ES modules로 서버 없이 열 수 있게 한다. 해시 라우팅을 사용해 프로젝트 하위 경로에서도 새로고침 오류가 없게 한다. 웹앱은 공개 JSON을 읽지만 GitHub에 직접 쓰거나 인증 토큰을 요구하지 않는다.

### 수집 및 일정 실행

수집은 정적 페이지가 아닌 로컬 작업으로 실행한다. 기본 루틴은 이 Codex 작업의 기존 예약 설정의 매일 07:00 Asia/Seoul 자동 실행이다. 사용자 컴퓨터/실행 환경이 이용 가능해야 하므로 실행 지연·누락과 마지막 성공 시각을 표시한다. 일정 기능을 만든 것과 실제 스케줄러 활성화는 구분한다.

GitHub Actions를 향후 사용하면 공개 수집에 한정한다. 웹훅 비밀번호, 개인 문서, 개인 메모는 클라우드에 저장하지 않는다. GitHub의 예약 실행은 지연될 수 있으며 정확한 예약 시각 실행을 보장하지 않는다. 게시 대상은 allowlist 파일만 사용하고 `git add .`로 작업 폴더 전체를 올리지 않는다.

### 알림

수집 결과의 신규/실질 변경/종료와 수집 장애를 기록한다. 단순 조회수, 페이지 공백, 수집 시각 변화는 업데이트로 보지 않는다. 실제 의미 있는 변경이나 장애가 있을 때 Codex 알림을 보내고, 변화가 없으면 조용히 종료한다. 공개 웹앱에는 업데이트함을 제공한다. 브라우저가 닫힌 상태에서 정적 사이트만으로 정시 푸시가 가능하다고 설명하지 않는다.

## 4. 초기 수집과 품질

기준 시각은 실행 시점에서 계산한다. 고정된 샘플 날짜를 실제 상태 판단에 사용하지 않는다.

- 기존 사용 채널: FCB, 네이버 kkbjob, 슈퍼루키, 네이버 cafe 15279069의 여러 관련 게시판, 금투협, 자소설닷컴, LinkedIn Jobs, 캐치, 원티드.
- 추가 채널: 운용사·증권사 공식 채용, Qraft, WorldQuant, 자산평가사, 지수/금융데이터 회사, 대학의 금융공학·수학·통계·산업공학·컴퓨터 취업게시판.
- 해외: Jane Street, Citadel/Citadel Securities, WorldQuant, 글로벌 운용사와 UBS 등 실제 관련 공식 채용의 지정 국가만 수집한다.
- 원티드의 특정 공고 링크는 탐색의 출발점이지 전체 채용목록이 아니다.
- 공개 API/공식 채용페이지를 우선한다. 사이트 정책과 속도 제한을 존중하고 CAPTCHA, 로그인, 접근 통제를 우회하지 않는다. 로그인 세션은 필요 시 로컬 브라우저에서만 사용한다.
- 첫 수집의 `전체`는 설정된 소스에서 검증 가능한 전체 범위를 뜻한다. 검색 제한, 접근 차단, 페이지 한도, 미지원 소스를 상태 화면에서 공개한다. 인터넷 전체 공고를 완전 수집했다고 주장하지 않는다.
- 목록의 제목만으로 마감일·신입 가능 여부를 확정하지 않는다. 상세 페이지를 확인한다. 첨부만 있는 공고는 추출 전까지 확인 필요다.
- 원문 미확인, 언어 불명, 지원 수준 불명은 `review`로 저장한다. 실제 미마감 근거와 신입/인턴 근거가 있는 공고만 `open`으로 표시한다.
- 마감은 원문 날짜/시각/시간대와 표시 문자열을 보존한다. 날짜만 있으면 시각 미상으로 표시하고 임의의 23:59를 확정 마감처럼 사용하지 않는다. 채용 시 마감은 `rolling`, 날짜 미상은 `unknown`이다.
- 공고가 목록에서 사라졌거나 요청이 실패했다고 즉시 마감 처리하지 않는다. 원문에 종료 증거가 있거나 검증된 마감 시각이 경과했을 때 마감 처리한다. 실패 시 이전 자료와 사용자 연결을 보존한다.
- 원문 채용 ID/공식 지원 URL을 우선 사용해 중복을 합친다. 회사와 제목만 같은 다른 채용은 합치지 않는다. 재게시 공고와 교차 채널 중복은 후보로 기록하고 근거 없이 병합하지 않는다.
- 전체 원문을 무단 재배포하지 않는다. 필수 사실과 짧은 요약·검증 근거만 공개 데이터에 저장하고 원문으로 연결한다.

## 5. 데이터 모델

공개 Job: id, sourceId, sourceUrl, applicationUrl, company, title, country, category, level, eligibility, languageStatus, visaStatus, postedAt, deadlineDate, deadlineAt, deadlineTimezone, deadlineText, deadlineKind, status, evidence, firstSeenAt, lastSeenAt, lastVerifiedAt, contentHash.

공개 Source: id, name, url, adapter, enabled, status, lastAttemptAt, lastSuccessAt, found, reason, coverageLimit.

공개 Change: id, jobId, kind(new/updated/closed), changedFields, detectedAt. 실제 변경 필드만 비교한다.

로컬 PersonalState: experiences[], documents[], favorites[], applications{}, events[], settings{}.

- Experience: id, title, situation, task, action, result, skills, dates. 수치·역할·성과를 사용자가 제공한 그대로 관리한다.
- Document: id, jobId(optional), company, role, kind(자소서/CV/Cover letter), question, content, experienceIds, createdAt, updatedAt. 저장은 명시적 버튼으로 한다.
- Event: id, jobId(optional), title, date, type(초안/검토/제출/개인), completed. 추후 일반 일정 확장이 가능하게 한다.
- Applications는 지원 상태와 개인 목표 제출일을 기록한다. 원문 마감일과 개인 목표일을 덮어쓰지 않는다.

## 6. 화면과 기능

1. 국내 공고: 검색, 분야, 상태 필터, 별표, 원문, 마감, 지원 수준·검증 상태. 최초 수집 진행/빈 결과/실패를 구별한다.
2. 해외 공고: 지정 5개국 필터, 언어·비자·학위 조건 표시. 현지어 필수 공고는 기본 후보에서 제외한다.
3. 달력: 월간 보기, 원문 마감일과 개인 작성 일정을 함께 표시. 별표 공고 강조, 마감 미정 목록 별도 표시. 공고를 열거나 초안 작성으로 연결한다.
4. 작성 도우미: 회사/직무/문서종류/문항을 입력하고 저장된 경험을 선택한다. 사실에 기반한 초안 구조를 구성하고 직접 편집·저장한다.
5. 경험 보관함: STAR 구조로 경험 추가·수정·삭제. 경험 삭제 시 저장 문서의 본문은 보존한다.
6. 내 지원서: 로컬 문서만 읽어 회사/직무별 목록 생성, 검색, 열기, 수정, 삭제, 평문 다운로드.
7. 수집 상태/설정: 소스별 접근 결과, 최종 수집·검증 시각, 변경 알림, 로컬 보관함 잠금, 암호화 백업/복원.

## 7. 작성 도우미의 AI 원칙

기본 기능은 외부 전송 없는 경험 기반 초안 구성과 편집이다. 생성형 AI가 연결되지 않은 상태에서 AI가 작성한 것처럼 표시하지 않는다. 선택한 경험만 사용하고 미제공 성과·수치·학위·역할·회사 사실을 창작하지 않는다. 확인할 부분은 `[확인 필요]`로 표시한다.

사용자는 후속 답변에서 로컬 AI가 없고 GPT를 사용하며 API 연결을 원한다고 밝혔다. 이에 GPT 요청문 복사와 로컬 서버를 통한 선택적 OpenAI Responses API 연결을 구현한다. API 키는 로컬 `.local/openai.json` 또는 환경변수에만 두고 브라우저·GitHub·로그에 노출하지 않는다. 요청 내용을 보여주고 사용자가 명시적으로 전송한 경우에만 선택한 자료를 전송한다. `store:false`를 사용하되 OpenAI 정책에 따른 보존 가능성을 안내한다. 외부 처리까지 금지하는 모드에서는 GPT 호출 없이 초안 구성·직접 편집만 사용한다. 로컬 서버는 127.0.0.1에만 바인딩하고 요청 크기, Origin, Host, 경로를 제한하며 입력/출력을 로그에 남기지 않는다. 공개 Pages에서 API를 사용하려면 로컬 웹앱을 열고 암호화 백업으로 자료를 옮긴다.

AI의 수정 제안은 사용자가 검토하고 반영한다. 생성이 실패하면 편집 내용을 유지한다. 영어 CV는 검증된 경험의 번역과 표현 개선을 지원하고 빈칸을 사실처럼 채우지 않는다.

## 8. 검증과 완료 기준

- 현재 시각·시차·날짜만 있는 마감·rolling·연도 경계 테스트.
- 신규/변경/종료/수집 실패 시 보존 및 중복 식별 테스트.
- 신입과 경력 혼합, 과장급, 언어 필수/우대, 미확인 학위 구분 테스트.
- 암호화 왕복, 잘못된 비밀번호, 변조된 백업 거부, 복원 실패 시 기존 데이터 보존 테스트.
- 저장 후 재열기, 기업/직무별 목록, 별표와 달력 연결, 경험 편집과 초안 저장 검증.
- 공고/개인 입력에 스크립트나 특수 URL을 넣어도 실행되지 않는지 검증.
- 빌드 결과에는 web allowlist 이외 파일, 개인자료, .env, 비밀번호, 로그가 없어야 한다.
- GitHub Pages 하위 경로에서 상대 URL과 새로고침이 정상이어야 한다.
- 실제 활성화된 일정과 실행 로그, 검증된 배포 URL이 없으면 자동화/배포가 완료됐다고 쓰지 않는다.

## 9. 구현 순서와 인수인계

1. 보관함·공개 데이터 분리와 정적 웹앱 구현.
2. 검증 가능한 소스 어댑터와 최초 수집. 미지원 소스는 숨기지 않는다.
3. 작성 도우미·달력·별표·문서 목록·백업 구현.
4. 로컬 AI 연결과 오류 처리.
5. 테스트 및 공개 산출물 검사.
6. 사용자가 지정한 GitHub 저장소에 공개 파일만 업로드 후 Pages 배포 확인.
7. 기존 로컬 예약 시각을 유지하고 최초 수동 실행을 검증한다.

확인된 GitHub 저장소는 `https://github.com/chososo/get_job`이며 신규 빈 저장소다. 로컬 AI는 없고 GPT API 연결을 준비하되 실제 키와 유료 API 실행은 아직 제공/검증되지 않았다. 다른 프로젝트 또는 기존 개인 파일을 임의로 업로드하지 않는다. 후속 개발자는 README와 실제 소스/테스트 결과를 먼저 읽고, 완료·미완료를 구별하여 작업을 이어간다.

## 공식 참고 문서

- GitHub Pages: https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages
- GitHub 예약 실행과 지연: https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule

## 2026-09-08 수집 연결 변경

API 키는 수집 현황·설정에서 한 번 저장하고 삭제할 수 있다. `.local/openai.json`은 로컬 프로그램만 사용하며 읽어서 대화·로그에 노출하지 않는다. 키 없음은 규칙 방식, 키 있음은 GPT-6 Astra. 새로고침과 루틴은 `scripts/refresh_jobs.py`를 공유한다. 대기업 관련 재무·IR·전략투자는 corporate 영역에 표시한다. 실제 접수 상태와 개인 조건 충족 여부를 혼동하지 않는다.
